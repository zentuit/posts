version: '2.4'

services: 
    posts:
        build: 
            context: .
            target: dev
        volumes:
            - .:/node/app:delegated
            - /node/app/node_modules
        command: node index.js -f ./in/posts.csv

        
        